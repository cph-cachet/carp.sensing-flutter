name: carp_context_package
description: CARP context sampling package. Samples location, activity, weather, and geofence.
version: 0.6.1
author:  CACHET Team <cph.cachet@gmail.com>
homepage: https://github.com/cph-cachet/carp.sensing-flutter/tree/master/carp_context_package

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^3.0.0
  carp_mobile_sensing: ^0.6.1
  weather: ^0.9.5
  location: ^2.3.5
  #activity_recognition: ^0.1.3
  activity_recognition_alt: ^0.1.7 # new alternative activity recognition based on the old one - maintain our own?
  openmhealth_schemas: ^0.1.4

# Overriding carp libraries to use the local copy
# Remove this when done development
dependency_overrides:
  carp_mobile_sensing:
    path: ../carp_mobile_sensing/
  openmhealth_schemas:
    path: ../../openmhealth_schemas/

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  json_serializable: ^3.0.0
  test:

# The following section is specific to Flutter.
flutter:
