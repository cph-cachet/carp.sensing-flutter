name: carp_communication_package_example
description: Examples of how to use the CANS Communication Package.
version: 0.40.0
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  carp_core:
    path: ../../../carp_core
  carp_mobile_sensing:
    path: ../../../carp_mobile_sensing
  carp_communication_package:
    path: ../

flutter:
  uses-material-design: true
