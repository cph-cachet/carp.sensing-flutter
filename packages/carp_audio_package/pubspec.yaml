# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec

name: carp_audio_package
description: CARP Audio Sampling Package. Samples audio recording and noise.
version: 0.9.5
author:  CACHET Team <cph.cachet@gmail.com>
homepage: https://github.com/cph-cachet/carp.sensing-flutter/tree/master/packages/carp_audio_package

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^3.0.0
  carp_mobile_sensing: ^0.9.5
  flutter_sound: ^5.0.0   # audio probe, recording audio
  noise_meter: ^1.2.0     # Measuring noise level
  path_provider: ^1.6.0
  stats: ^1.0.0
  permission_handler: ^5.0.1+1

# Overriding carp libraries to use the local copy
# Remove this before release of package
#dependency_overrides:
#  carp_mobile_sensing:
#    path: ../../carp_mobile_sensing/

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.0
  json_serializable: ^3.0.0
  test:

# The following section is specific to Flutter.
flutter:
