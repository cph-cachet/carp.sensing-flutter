name: carp_context_package_example
description: A simple example of using the CARP Context Package
publish_to: 'none' 

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  carp_core: ^1.8.0
  carp_mobile_sensing: ^1.10.0
  
  # carp_core:
  #   path: ../../../carp_core/
  # carp_mobile_sensing:
  #   path: ../../../carp_mobile_sensing
  carp_audio_package:
    path: ../

flutter:
  uses-material-design: true
